# Archivo de variables de entorno
# Copiar este archivo a .env y completar los valores

# Seguridad
SECRET_KEY=django-insecure-smartconnect-key-2024-cambiar-en-produccion-AHORA
DEBUG=False

# Hosts permitidos (separar con comas)
ALLOWED_HOSTS=98.88.189.220,tu-dominio.com,localhost,127.0.0.1

# Base de datos SQLite (cambiar a PostgreSQL en producción)
# Para SQLite, dejar como está
# Para PostgreSQL:
# DATABASES_ENGINE=django.db.backends.postgresql
# DATABASES_NAME=smartconnect_db
# DATABASES_USER=smartconnect_user
# DATABASES_PASSWORD=tu-password-seguro
# DATABASES_HOST=localhost
# DATABASES_PORT=5432

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://98.88.189.220

# JWT
JWT_SECRET_KEY=tu-jwt-secret-key-aqui
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_LIFETIME=3600
JWT_REFRESH_TOKEN_LIFETIME=86400

# Email (opcional, para recuperación de contraseña)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu-email@gmail.com
# EMAIL_HOST_PASSWORD=tu-app-password

# ============================================
# CONFIGURACIÓN DE DOCMAN API (OPCIONAL)
# ============================================
# Si integras con Docman API para gestión de documentos
DOCMAN_API_URL=
DOCMAN_API_KEY=
DOCMAN_TIMEOUT=30

# ============================================
# NOTAS IMPORTANTES PARA AWS EC2
# ============================================
# 1. Asegúrate de cambiar DEBUG=False en producción
# 2. Genera una SECRET_KEY única y segura
# 3. Agrega la IP pública de tu EC2 a ALLOWED_HOSTS
# 4. Ejecuta: python manage.py collectstatic --noinput
# 5. Ejecuta: python manage.py migrate
# 6. Crea superusuario: python manage.py createsuperuser
